{
  "name": "@libticketto/switch-default",
  "version": "0.0.0",
  "private": true,
  "description": "Forbids a switch over Command kinds or TickettoErrorCode without a default (features/002-sdk/plan.md §5.6)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "switch-default": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "oxc-parser": "0.149.0"
  },
  "devDependencies": {
    "@types/node": "24.13.4"
  }
}
