{
  "name": "@ticketto/ledger-rules",
  "version": "0.0.0",
  "description": "The ledger's rules — commands, invariants and errors — implemented once over the capability interfaces, and run by whatever holds authoritative ledger state.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KippuRocks/libticketto.git",
    "directory": "packages/ledger-rules"
  },
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=24"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ticketto/sdk": "workspace:*"
  }
}
