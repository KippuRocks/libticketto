{
  "name": "@ticketto/backend-memory",
  "version": "0.0.0",
  "description": "The in-memory reference backend, independent of the hosted one, and the slow-backend decorator.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KippuRocks/libticketto.git",
    "directory": "packages/backend-memory"
  },
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=24"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ticketto/ledger-rules": "workspace:*",
    "@ticketto/sdk": "workspace:*"
  },
  "devDependencies": {
    "@ticketto/profile-v0": "workspace:*"
  }
}
