{
  "name": "@ticketto/conformance",
  "version": "0.0.0",
  "description": "The shared conformance suite, written once against the SDK surface and run against every backend.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KippuRocks/libticketto.git",
    "directory": "packages/conformance"
  },
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=24"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ticketto/profile-v0": "workspace:*",
    "@ticketto/sdk": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "5.0.0"
  },
  "devDependencies": {
    "vitest": "5.0.0"
  }
}
