---
"@ticketto/log": minor
---

`C7` log record: `encodeRecord`, `decodeRecord` and `hashRecord` under the `ticketto/v0/log` domain tag, with the `NFR-6` allow-list refusing any field it does not name (T-006-01).
