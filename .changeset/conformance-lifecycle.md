---
"@ticketto/conformance": minor
---

Event lifecycle suites — `INV-4`, `INV-11`, `INV-16` and their errors — with every test tagged by the milestone whose rules it needs. `defineConformance` and `runSuites` now take `{ through: "M<n>" }` (T-004-03).
