---
"@ticketto/log": minor
---

The published `C7` format document, `FORMAT.md`, and test vectors `vectors/v0.json`, shipped with the package and reproduced by an independent decoder written from the document alone (T-006-06).
