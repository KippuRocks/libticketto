---
"@ticketto/sdk": minor
---

`getCredential(account, credential)`: a `GetCredential` query answering the `Registration` of a credential registered to an account, or `null`, so a verifier other than the ledger can check a holder's proof of control against the ledger's record (T-002-12).
