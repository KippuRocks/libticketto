---
"@ticketto/backend-memory": minor
---

The in-memory backend's `migration` member: export and import in `@ticketto/log`'s format, the checkpoint signed by a `publication` key, zones in use and receipts recomputed on import (T-005-06, REQ-MG-3).
