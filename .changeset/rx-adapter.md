---
"@ticketto/rx": minor
---

`fromSubmission` and `fromHints`: a `Submission` as an `Observable<SubmissionState>` and a log reader's hints as an `Observable<Cursor>`, with RxJS as a peer dependency (T-002-11).
