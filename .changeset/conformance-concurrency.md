---
"@ticketto/conformance": minor
---

Concurrency variant — `AC-E3.2`, `INV-6` and `INV-4` over 1,000 randomised runs of parallel submissions through the port — and per-test timeouts; `collectTests`, `runTest` and `runsThrough` exported for running suites outside Vitest's registration (T-004-08).
