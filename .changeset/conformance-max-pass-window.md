---
"@ticketto/conformance": minor
"@ticketto/backend-memory": minor
---

`ERR-PassExpired` for a pass window longer than the maximum pass window, and accepted at it; `TestControls` gains optional `maxPassWindow`, which backend-memory's `/testing` reports (follow-up to T-004-06, T-008-17).
