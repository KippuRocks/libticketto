---
"@ticketto/conformance": minor
"@ticketto/backend-memory": minor
---

Gate suites — `INV-3`, `INV-5`, `INV-6`, `INV-8` (attendance), `ERR-InvalidPass`, `ERR-PassExpired`, `ERR-PassReplayed`, `ERR-CannotAttend`, `ERR-TicketExpired` — tagged by milestone; `TestControls` gains `maxRecordingLag` and `maxClockSkew`, which backend-memory's `/testing` reports; `ERR-PolicyUndeterminable` moves out of the suite (T-004-06).
