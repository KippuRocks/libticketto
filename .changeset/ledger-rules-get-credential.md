---
"@ticketto/ledger-rules": minor
---

`getCredential` query (T-008-15): the registration stored for `(account, credential)`, or `null` when that credential is not registered to that account. A point lookup; nothing enumerates an account's credentials (`REQ-MG-5`).
