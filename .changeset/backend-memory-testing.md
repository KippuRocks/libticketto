---
"@ticketto/backend-memory": minor
---

`@ticketto/backend-memory/testing`: `createTestMemoryBackend` with a controlled, monotonic clock the rules read and seeded random bytes — the conformance suite's `TestControls` (T-005-05, REQ-SDK-7).
