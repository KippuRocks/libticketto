---
"@ticketto/profile-v0": minor
---

`C2` test vectors, checked in as `vectors/v0.json` and shipped with the package, with a generator and a CI check that fails when codecs change without regenerated vectors (T-003-08).
