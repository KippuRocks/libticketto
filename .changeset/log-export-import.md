---
"@ticketto/log": minor
---

Export and import (`FORMAT.md` §5): `encodeExport`, `exportStream` and `readExport` for the export stream and snapshot, and the import driver `importVerified` with `verifyImport`, which checks a backend's log and point queries against the export through the port alone (T-006-04).
