---
"@ticketto/conformance": minor
---

`assuranceProblems` and the `REQ-SDK-6` suite: a backend's assurance declaration must have an `enforced` or `attested` entry for every live invariant of §9, and nothing else (T-004-10).
