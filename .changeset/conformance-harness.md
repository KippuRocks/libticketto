---
"@ticketto/conformance": minor
---

Conformance harness: `defineConformance` over a `ConformanceTarget` (a backend in test mode, a profile, its signers and identifiers), the `TestControls` contract — a settable clock and seeded randomness — and V0 profile fixtures (T-004-01).
